[tox]
envlist = py36

[testenv]
whitelist_externals=
  rm
  make

commands=
    ; Force packaging even if setup.{py,cfg} haven't changed
    rm -rf ./diff_register.egg-info/
    python setup.py ci

deps=
    -requirements.txt
